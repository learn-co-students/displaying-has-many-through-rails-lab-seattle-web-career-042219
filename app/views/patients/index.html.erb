<h2>All Patients</h2>

<ul>
	<% @patients.each.with_index(1) do |patient, i| %>
	<li>
		<%= i %>: Name: <%= link_to patient.name, patient %><br>
		Number of Appointments: <%= patient.appointments.count %>
	</li>
	<% end %>
</ul>
